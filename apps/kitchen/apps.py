"""Configuration for the kitchen app."""
from django.apps import AppConfig


class KitchenConfig(AppConfig):
    """Configuration for the kitchen app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.kitchen"
